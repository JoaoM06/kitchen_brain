# `mobile/src/theme`

Design tokens used across the app.

- `colors.jsx` – Palette containing primary brand greens, background neutrals, overlay colors, etc.

Components should import from here instead of hardcoding values so theming can change centrally.
