# `mobile/src/utils`

Utility helpers that do not fit other folders.

- `menuPdf.js` – Generates PDF exports for the menu builder using Expo Print/Sharing modules.

Add future utils here (date formatting, analytics wrappers, etc.) to keep screens lean.
